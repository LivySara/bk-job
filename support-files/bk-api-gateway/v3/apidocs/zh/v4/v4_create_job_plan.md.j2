### 功能描述

在指定资源范围下，基于某作业模板创建新的执行方案。业务校验、权限与落库流程与「Web 端创建执行方案」一致，仅通过 OpenAPI V4 对外暴露契约（请求体为 JSON，字段为 snake_case）。

### 能力范围（产品确认）

**仅支持在已有作业模板下新建执行方案**：必须提供已存在且有权访问的 `job_template_id`；不支持无模板、仅从草稿或独立 JSON 全量描述步骤等场景。若后续扩展此类能力，需另立规格与接口。

### 请求参数

#### Body参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| bk_scope_type | string | 是 | 资源范围类型。可选值: biz - 业务，biz_set - 业务集 |
| bk_scope_id | string | 是 | 资源范围ID, 与bk_scope_type对应, 表示业务ID或者业务集ID |
| job_template_id | long | 是 | 作业模板 ID，必须已存在且调用方有创建执行方案权限 |
| name | string | 是 | 执行方案名称；校验规则与 Web 一致：去首尾空格、不可为空、不可含非法字符、最大长度 60 |
| enable_steps | array | 是 | 启用的作业模板步骤 ID 列表，对应创建时从模板步骤映射的 template_step_id（与 Web 端「启用的步骤」一致） |
| variables | array | 否 | 用于覆盖作业模板全局变量默认值；与 Web `TaskPlanCreateUpdateReq.variables` 语义一致。不传或传空数组表示不覆盖，全部使用模板侧变量定义 |

**variables 数组元素**（与 Web 执行方案变量结构一致，字段名 snake_case）:

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| id | long | 条件 | 模板全局变量 ID；需要覆盖该变量默认值时必传（与 buildPlanInfo 中按 ID 应用覆盖一致） |
| name | string | 条件 | 变量名，与 Web 端创建时要求一致 |
| type | int | 条件 | 变量类型 1-字符串 2-命名空间 3-主机列表 4-密码 5-关联数组 6-索引数组 |
| default_value | string | 否 | 非主机列表类型时的默认值 |
| default_target_value | object | 否 | 主机列表类型时，与 Web 端 `TaskTargetVO` 结构一致；嵌套字段命名与现网 API 习惯一致（如 executeObjectsInfo 等） |
| value | string | 否 | 同 Web 变量值字段 |
| target_value | object | 否 | 主机类变量值 |
| description | string | 否 | 描述 |
| changeable | int | 条件 | 0-不可变 1-可变 |
| required | int | 条件 | 0-非必填 1-必填 |
| delete | int | 否 | 0-不删除 1-删除，创建方案场景一般不用 |

### 请求参数示例

- POST

```json
{
    "bk_scope_type": "biz",
    "bk_scope_id": "2",
    "job_template_id": 1000,
    "name": "my-api-plan",
    "enable_steps": [1, 2, 3],
    "variables": []
}
```

### 返回结果示例

#### 失败示例

{% include '_generic_v4_error_resp.md.j2' %}

#### 成功示例

```json
{
    "data": {
        "bk_scope_type": "biz",
        "bk_scope_id": "2",
        "bk_biz_id": 2,
        "job_plan_id": 50001,
        "job_template_id": 1000,
        "name": "my-api-plan"
    }
}
```

### 返回结果参数说明

{% include '_generic_v4_response.md.j2' %}

#### data

| 字段 | 类型 | 描述 |
|------|------|------|
| bk_scope_type | string | 资源范围类型 |
| bk_scope_id | string | 资源范围 ID |
| bk_biz_id | long | 兼容字段，业务/业务集 ID |
| job_plan_id | long | 新建执行方案 ID |
| job_template_id | long | 作业模板 ID |
| name | string | 执行方案名称 |
